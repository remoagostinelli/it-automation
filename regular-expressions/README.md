# Working with Regular Expressions
Find the users using an old email domain in a big list using regular expressions.
Replace the old domain name (abc.edu) with a new domain name (xyz.edu).
Store all domain names, including the updated ones, in a new file.
